<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MauiAppDemo.Pages"
             x:Class="MauiAppDemo.Pages.MainPage"
             Loaded="ContentPage_Loaded">

    <ContentPage.BindingContext>
        <local:MainPageViewModel />
    </ContentPage.BindingContext>

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="ログアウト" Command="{Binding LogoutCommand}" />
    </ContentPage.ToolbarItems>

    <ScrollView VerticalOptions="Center" HorizontalOptions="Center" Margin="5,5,5,5">
        <VerticalStackLayout>

            <Label Text="{Binding No}" />

        </VerticalStackLayout>
    </ScrollView>
 
</ContentPage>
